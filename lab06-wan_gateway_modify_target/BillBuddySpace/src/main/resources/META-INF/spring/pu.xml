<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:os-core="http://www.openspaces.org/schema/core"
       xmlns:os-events="http://www.openspaces.org/schema/events"
       xmlns:os-remoting="http://www.openspaces.org/schema/remoting"
       xmlns:os-gateway="http://www.openspaces.org/schema/core/gateway"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
       http://www.openspaces.org/schema/core http://www.openspaces.org/schema/core/openspaces-core.xsd
       http://www.openspaces.org/schema/events http://www.openspaces.org/schema/events/openspaces-events.xsd
       http://www.openspaces.org/schema/remoting http://www.openspaces.org/schema/remoting/openspaces-remoting.xsd
       http://www.openspaces.org/schema/core/gateway http://www.openspaces.org/schema/core/gateway/openspaces-gateway.xsd">

    <!-- Shared by both sites (US and EMEA), deployed twice (once per site)
         with different -p overrides, instead of maintaining two near-identical
         folders. Defaults below are only used if a value isn't passed at deploy time.

         Unlike lab01-lab05, this module deploys IDENTICALLY on both sides:
         gatewayTargets below has zero <os-gateway:target> children on both US
         and EMEA. The space is still gateway-enabled (it has a real, if empty,
         target list, so ReplicationManager.addGatewayTarget/removeGatewayTarget
         are usable against it), but out of the box neither side actively
         replicates anywhere - even though BillBuddyGateway's delegator/sink are
         fully deployed and CONNECTED to each other from the start. This lab is
         entirely about using the Admin API (see ModifyGatewayTarget) to add and
         remove entries from this list at runtime, without ever redeploying this
         PU. Confirmed against the openspaces-gateway.xsd shipped in
         xap-openspaces: <os-gateway:target> is minOccurs="0" inside
         <os-gateway:targets>, so an empty list is valid, schema-checked XML,
         not a workaround. -->

    <bean id="propertiesConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="ignoreResourceNotFound" value="true" />
        <property name="properties">
            <props>
                <prop key="localSpaceName">wanSpaceUS</prop>
                <prop key="localGatewayName">US</prop>
            </props>
        </property>
    </bean>

    <context:annotation-config />

    <tx:annotation-driven transaction-manager="transactionManager" />

    <os-core:distributed-tx-manager id="transactionManager" />

    <!-- Enable support for @Polling annotation -->
    <os-events:annotation-support />

    <os-remoting:annotation-support />

    <os-core:embedded-space id="space" space-name="${localSpaceName}" mirrored="false" gateway-targets="gatewayTargets" />
    <os-core:giga-space id="gigaSpace" space="space" tx-manager="transactionManager" />

    <os-core:giga-space-context />

    <os-gateway:targets id="gatewayTargets" local-gateway-name="${localGatewayName}" />
</beans>
